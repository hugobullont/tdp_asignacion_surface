<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Drive</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <fieldset>
        <% if (images != null) {%>
          <h2>Images</h2>
          <% for (var i = 0; i < images.length; i++){%>
          <a href= <%= images[i].link %>><img width="300" src= <%=images[i].link %> ></a>
          <% } }%>
        <% if (videos != null) {%>
          <h2>Videos</h2>
          <% for (var i = 0; i < videos.length; i++){%>
          <a href= 'https://youtu.be/<%=videos[i].id.videoId %>'><img src= <%=videos[i].snippet.thumbnails.medium.url %> ></a>
          <!--<p><%= videos[i].snippet.title %></p>-->
          <% } }%>
      </fieldset>
    </div>
  </body>
</html>
